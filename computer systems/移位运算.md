移位运算是指将二进制的数的各个位置上的元素左移或右移的操作。

左移：将低位补0即可，其最终结果 = 原值 \* ，N表示移动的位数

右移：如果是带符号的数值，则需要判断是否为负数，如果为负数则需要在空出的位置上补1，如果为正数则需要在空出来的位置上补0。

其最终结果 = 原值 / ，N表示移动的位数，结果取整数

符号位扩展：将数据进行符号位扩展是为了产生一个位数加倍但数值大小不变的结果。从4位扩充到8位。如果是负数则在空出的位置补1，如果是正数则补0